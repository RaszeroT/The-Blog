<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Blog | Home</title>
</head>
<body>
    {{!-- header --}}
    <header>
        <h1>
            <a href="/">The Blog</a>
        </h1>
        <nav>
            {{#if logged_in}}
            <a href="/profile">Profile</a> |
            <button class="no-btn">Logout</button>
            {{else}}
            <a href="/login">Login</a>
            {{/if}}
        </nav>
    </header>
    {{!-- main --}}
    <section id="main">
        {{{body}}}
    </section>
    <footer>

    </footer>

      <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html>